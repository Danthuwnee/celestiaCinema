spring:
  datasource:
    url: jdbc:h2:file:./data/cinema_db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
  sql:
    init:
      mode: never
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
  mail:
    host: smtp.gmail.com
    port: 587
    username: nhutt859@gmail.com
    password: ysab nmia dlnv zzbs
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        transport:
          protocol: smtp

app:
  redis:
    enabled: false
    seat-lock-ttl: 900
    max-seats-per-booking: 8

logging:
  level:
    com.cinema: DEBUG
    org.hibernate.SQL: DEBUG
